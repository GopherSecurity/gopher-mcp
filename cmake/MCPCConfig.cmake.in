@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find required dependencies
find_dependency(Threads)
find_dependency(OpenSSL)

# Include the targets file
include("${CMAKE_CURRENT_LIST_DIR}/MCPCTargets.cmake")

# Check that all required components are available
check_required_components(MCPC)

# Provide library paths for convenience
set(MCPC_LIBRARIES MCP::mcp_c)
set(MCPC_STATIC_LIBRARIES MCP::mcp_c_static)
set(MCPC_INCLUDE_DIRS "@PACKAGE_CMAKE_INSTALL_INCLUDEDIR@")