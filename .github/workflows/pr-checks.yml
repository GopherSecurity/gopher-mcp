# Copyright 2025 Gopher Security, Inc.
# SPDX-License-Identifier: Apache-2.0

name: PR Checks

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  clang-format:
    name: Clang Format
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    
    steps:
    - name: Checkout PR
      uses: actions/checkout@v3
      with:
        # Fetch the PR branch and the base branch
        fetch-depth: 0
        
    - name: Install clang-format
      run: |
        sudo apt-get update
        sudo apt-get install -y clang-format-14
        
    - name: Check changed files
      id: changed-files
      run: |
        # Get list of changed C++ files
        git diff --name-only origin/${{ github.base_ref }}...HEAD | grep -E '\.(h|cpp)$' > changed_files.txt || true
        
        if [ -s changed_files.txt ]; then
          echo "has_changes=true" >> $GITHUB_OUTPUT
          echo "Changed C++ files:"
          cat changed_files.txt
        else
          echo "has_changes=false" >> $GITHUB_OUTPUT
          echo "No C++ files changed"
        fi
        
    - name: Check formatting of changed files
      if: steps.changed-files.outputs.has_changes == 'true'
      run: |
        exit_code=0
        while IFS= read -r file; do
          if [ -f "$file" ]; then
            echo "Checking $file..."
            clang-format-14 --style=file --dry-run --Werror "$file" || {
              echo "::error file=$file::File is not properly formatted"
              exit_code=1
            }
          fi
        done < changed_files.txt
        
        if [ $exit_code -ne 0 ]; then
          echo ""
          echo "::error::Some files are not properly formatted."
          echo "To fix, run: make format"
          exit 1
        fi
        
    - name: Post PR comment on failure
      if: failure() && steps.changed-files.outputs.has_changes == 'true'
      uses: actions/github-script@v6
      with:
        script: |
          const comment = `## ❌ Code Formatting Check Failed
          
          Some files in this PR are not properly formatted according to the project's clang-format rules.
          
          **To fix this issue:**
          \`\`\`bash
          make format
          \`\`\`
          
          Then commit and push the changes.`;
          
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: comment
          });
  license-header:
    name: License Header
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
    - name: Checkout PR
      uses: actions/checkout@v3
      with:
        # Need the base branch as well so the diff below can be computed.
        fetch-depth: 0

    - name: Check headers on changed files
      run: |
        # Only the PR's own files are checked. Existing files were covered by
        # the initial header pass, so a full-tree scan here would just make
        # every PR pay for unrelated history.
        git diff --name-only --diff-filter=d \
          origin/${{ github.base_ref }}...HEAD > changed_files.txt || true

        if [ ! -s changed_files.txt ]; then
          echo "No files changed."
          exit 0
        fi

        xargs ./scripts/check-license-headers.sh < changed_files.txt
